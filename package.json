{
  "name": "playwright_automation_project",
  "type": "module", 
  "_comment": "Using ES modules, so we added type: module",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test tests/webApiPartOne1.spec.js",
    "regression": "npx playwright test",
    "webTests": "npx playwright test --grep \"@Web\"",
    "allure:report": "allure generate ./allure-results --clean -o ./allure-report && allure open ./allure-report",
    "_comment":"npx cucumber-js --tags='@Regression' --retry 1 --exit --format html:cucumber-report.html",
    "CucumberRegression": "npx cucumber-js --tags=@Regression --retry=1 --exit --format=html:cucumber-report.html"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^24.0.1",
    "allure-playwright": "^3.3.0"
  },
  "dependencies": {
    "@cucumber/cucumber": "^11.3.0",
    "exceljs": "^4.4.0"
  }
}
